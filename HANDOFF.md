# 何切るAI 引き継ぎメモ

最終更新: 2026-09-20（ベースライン測定まで完了） / ブランチ: `feature/nanikiru-ai`（main へ未マージ）

このファイルは**別セッションで作業を再開するための入口**。
設計の詳細は [nanikiru-ai-design-doc.md](./nanikiru-ai-design-doc.md)、開発ルールは [AGENTS.md](./AGENTS.md) を見る。

---

## 1. 何を作っているか

麻雀の「何切る」に答えるAI。既存アプリ `mahjong416` への機能追加。

誤りを3層に分けて、層ごとに手段を変えているのが設計の核。

| 層 | 内容 | 手段 | 状態 |
| --- | --- | --- | --- |
| 計算層 | シャンテン数・受入枚数・ドラ枚数 | **自実装のソルバー**で厳密に計算し、プロンプトに事実として注入 | 完了 |
| 判断層 | 最終形・手役・打点のトレードオフ | **Vertex AI のプリファレンス チューニング（DPO）** | データ収集中 |
| 打点層 | 役・翻数 | 未着手（ドラ枚数のみ渡している） | 保留 |

**LLMに計算をさせない**のが最重要の原則（AGENTS.md ルール10）。

---

## 2. いまどこまで進んだか

```
コミット 39件 / テスト 273件・typecheck すべてGreen / 問題データ 10件
```

| フェーズ | 状態 | 備考 |
| --- | --- | --- |
| 1. ソルバー | ✅ 完了 | 牌パース・手牌検証・シャンテン数・受入・ドラ・全候補評価 |
| 2. データ入力基盤 | ✅ 完了 | 問題マスタ・検証コマンド・入力スキル |
| 4. データ変換 | ✅ 完了 | `npm run dataset` で DPO 用 JSONL を生成 |
| 5. 評価ハーネス | ✅ 完了 | `npm run eval`。実機で動作確認済み |
| 6. API | 🟡 実行フロー完成 | **HTTP入口（Cloud Functions のエントリ）が未実装** |
| 0. ベースライン測定 | ✅ 完了 | 厳格正解率20% / 数値整合率100%。詳細は [4](#4-ベースライン測定の結果) |
| 3. データ整備 | 🟡 10/300問 | ユーザーが自然言語で提供 → スキルで登録 |
| 7. フロントエンド | ⛔ 未着手 | `mahjong-nanikiru` タブの追加 |
| チューニング実行 | ⛔ 未着手 | GCS アップロード・ジョブ起動スクリプトが未実装 |

---

## 3. 主要なコマンド

```bash
# 開発（functions/ 配下）
cd functions
npm test                       # ユニットテスト
npm run test:watch             # TDD中はこれ
npm run typecheck              # 型チェック（テストも対象）

# データ
npm run check -- ../data/problems/0001.json   # 問題の検証とソルバー結果の表示
npm run dataset                               # data/build/ に JSONL を生成

# チューニング（GOOGLE_CLOUD_PROJECT と NANIKIRU_TUNING_BUCKET が必要）
npm run pick-test -- --count 8 --seed nanikiru-v1  # テストセットの選定（表示のみ）
npm run tune -- --label v1                         # GCSへ上げてジョブ投入
npm run tune:status -- <ジョブ名>                   # 状態とエンドポイント

# 測定（GOOGLE_CLOUD_PROJECT=mahjong416 が必要）
# NANIKIRU_MODEL にエンドポイントを渡すとチューニング済みモデルを測れる
npm run eval -- --split train --verbose       # ソルバー結果あり、理由も表示
npm run eval -- --split train --no-solver     # ソルバー結果なし
npm run eval -- --limit 3                     # 件数を絞る（課金を抑える）
```

ルートからは `npm run test:solver` / `npm run test:solver:watch` でも同じ。

---

## 4. 測定の結果

### 4.1 テストセット（8問）

`npm run pick-test -- --count 8 --seed nanikiru-v1` で選定。**以後この8問は学習に使わない。**

```
0005 0008 0016 0018 0020 0022 0026 0027
```

### 4.2 第1回チューニングの結果（2026-09-24）

学習22ペア、`gemini-2.5-flash` の PREFERENCE_TUNING。同じテスト8問で前後を測定した。

| 指標 | チューニング前 | チューニング後 |
| --- | --- | --- |
| 厳格正解率 | 37.5%（8問中3問） | 28.6%（7問中2問） |
| 準最善一致率 | 100.0% | 100.0% |
| 悪手回答率 | 12.5% | 14.3% |
| 数値整合率 | 100.0% | 100.0% |

チューニング後は1問（0005）が通信失敗で指標から除外された。**共通の7問で揃えると 42.9% → 28.6%。**

正誤の入れ替わりは3問だけ。

| 問題 | 前 | 後 |
| --- | --- | --- |
| 0008 | 誤 | **正** |
| 0018 | 正 | **誤** |
| 0020 | 正 | **誤** |

**結論: 改善は確認できなかった。** ただし悪化とも言い切れない。7問で3問入れ替わっただけなので、偶然の範囲を出ない。

判断材料として重要なのは次の点。

- **学習量が足りていない。** 22ペアは、Googleが教師ありチューニングで推奨する「まず100件」を大きく下回る
- **挙動は変わっていない。** 準最善一致率は前後とも100%、誤答はすべて「牌効率は妥当」。ソルバーの最良手をなぞる癖は矯正できていない
- **0026 は前後とも悪手（6s）を回答した。** 手動で指定した悪手がそのまま出ており、DPOで狙った矯正が効いていない

**この回の価値は精度ではなくパイプラインの確立にある。** データ生成 → GCS → チューニング → エンドポイントでの測定が、一通り動くことを確認できた。

### 4.3 参考: 初回の測定（2026-09-20・学習データ10問で自己採点）

```
厳格正解率: 20.0% / 準最善一致率: 90.0% / 悪手回答率: 10.0% / 数値整合率: 100.0%
```

学習データ自身を測ったものなので、4.2 とは比較できない。数値整合率100%が**計算層は解決済み**であることを示している点だけが有効。

---

## 5. 決まっていること（議論済み・蒸し返さない）

| 項目 | 結論 | 経緯 |
| --- | --- | --- |
| 学習手法 | **DPO（プリファレンス チューニング）** | SFTも検討したが、ソルバーという「牌効率しか見ない対抗馬」を悪手に据えられるため、対比を学習できるDPOを選んだ |
| 悪手データ | **ソルバーの最良手をそのまま悪手にする** | 正解と一致する場合は次の候補。人手不要 |
| 許容手の扱い | 解説が許容手として挙げた打牌は**悪手にしない**。`rejected` を手動指定して避ける | 問題0003で実際に発生 |
| 出力項目 | **推奨打牌 + 理由のみ**。「避けるべき打牌」は出力しない | 悪手1件につき2種類の文章が必要になり工数が倍になるため |
| 次善手 | **ソルバーから機械的に付加**。LLMには生成させない | `acceptable` のデータが無く、幻覚のリスクが高い |
| ツモ牌 | 手牌と分けて `draw` フィールドに持つ | 原典が「13枚＋ツモ」形式。副露直後（ツモなし）も表現できる |
| 食い替え | **判定しない**（非ゴール） | チー直後にしか影響せず、負担に見合わない |
| ブランチ | `feature/nanikiru-ai` で開発。成功基準を満たすまで main へマージしない | |

---

## 6. 残タスク

### 優先度：高

1. **問題データの収集（10/300問）**
   - ユーザーが自然言語で提供 → `nanikiru-data-entry` スキルで登録
   - 登録のたびに `npm run check` でソルバーと突き合わせる（**出典の数字との食い違いはバグ発見の手がかりになる**。実際に1件発見した）

2. **テストセットの確保**
   - 60〜80問たまった時点で、**固定シードによる無作為抽出で30問**を `split: test` にする
   - 一度決めたら変更しない。変えると過去の測定値と比較できなくなる

### 優先度：中

3. **チューニング実行のスクリプト**
   - `data/build/*.jsonl` を GCS にアップロード
   - チューニングジョブを起動し、完了を待つ
   - `score` の 0/1 の意味づけ（1 = preferred）を少数データで実証してから本番を回す

4. **HTTP入口の実装**
   - `functions/src/index.ts` に Cloud Functions のエントリを作る
   - `firebase-functions` の依存追加が必要
   - 実行フロー（`runNanikiru`）は完成済みなので、HTTPの受け口を被せるだけ

5. **フロントエンド**
   - `src/components/mahjong-nanikiru.ts` を新規追加
   - 既存コンポーネントには手を入れない

### 優先度：低

6. 役・翻数の計算（打点層）
7. 三人麻雀への対応

---

## 7. 既知の課題

### 推論が遅い

実測で**10問に37分（1問あたり約3.7分）**。テストセット30問なら約2時間、300問なら丸1日かかる。
Gemini 2.5 Flash の思考トークンが要因と見られる。`thinkingConfig` での調整を検討する。
測定のたびに数時間かかる状態だと、チューニングの試行錯誤が回らない。**優先して対処すべき課題。**

### 通信が無反応のまま止まることがある

`fetch` に既定のタイムアウトが無く、接続が死んでも例外が飛ばずに永久に待つ。
実際に測定が43分ハングした（`852bf7f` で対処済み。180秒のタイムアウトと再試行を入れた）。
長時間の処理を書くときは、**例外が飛ばずに止まる可能性**を常に考える。

### ソルバーの答えが実は許容手だった場合のリスク

悪手を自動生成する方式の弱点。正しい打牌を悪手として学習させてしまう。
学習後に「妙に牌効率を嫌う」挙動が出たらこれを疑う。`rejected` の手動指定で個別に上書きできる。

### この10問は学習データでもある

チューニング後に同じ10問で測っても意味のある比較にならない。テストセットの確保（残タスク3）が前提。

### `functions/tsconfig.json` はテストも型チェックの対象

ビルドは `tsconfig.build.json`（テスト除外）を使う。`npm run build` と `npm run typecheck` で対象が違う。

---

## 8. 落とし穴（踏むと時間を失う）

- **ソルバーの計算を信じすぎない。** 出典の数字と食い違ったら、まずソルバーを疑う。実際に「面子を取るときにブロック数上限を見ていない」バグがあり、受入枚数が過大になっていた（`9bb2586`）
- **`npm run eval` は課金が発生する。** `--limit` で件数を絞ってから本番を回す
- **ADC はマシン全体で共有される。** 現在 `itagaki.shintaro@gmail.com` / quota project `mahjong416` に設定済み。業務用の GCP を使う場合は影響する
- **学習と推論でプロンプトを一致させる。** `buildSystemInstruction` / `formatAnswer` が唯一の定義。ずれるとチューニングの効果が出ない
- **テストセットの分割を後から変えない。** 測定値が比較できなくなる

---

## 9. ディレクトリ

```
functions/src/
├── solver/       シャンテン数・受入・ドラ・全候補評価（外部依存なしの純粋関数）
├── prompt/       プロンプト構築・出力の整形とパース
├── api/          リクエスト検証・実行フロー・レスポンス組み立て
├── vertex/       Vertex AI クライアント（再試行つき）
├── data/         問題マスタ・悪手の自動生成・JSONL生成
├── eval/         評価指標・測定コマンド
├── situation.ts  局面の検証
└── json.ts       JSON読み取りヘルパー

data/problems/    問題マスタ（正本・コミットする）
data/build/       生成物（gitignore）
.claude/skills/nanikiru-data-entry/   問題登録スキル
```
