# 何切るAI Design Doc

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-09-17 |
| ステータス | Draft（レビュー待ち） |
| 対象リポジトリ | `mahjong416` |
| 手法 | Vertex AI Gemini プリファレンス チューニング（DPO） |

---

## 1. 目的とゴール

### 1.1 背景

汎用LLMに麻雀の「何切る」を解かせると精度が低い。原因は大きく2つに分かれる。

1. **計算層の誤り** — シャンテン数・受け入れ枚数の数え上げを間違える
2. **判断層の誤り** — 牌効率と打点・速度のトレードオフ、セオリーの適用を誤る

本プロジェクトでは、**1はソルバーの自実装で構造的に排除**し、**2をプリファレンス チューニング（DPO）で改善**する。

### 1.2 なぜプリファレンス チューニングか

何切るには唯一の正解がない。「この打牌は許容、これは明確に悪手」という優劣の構造を持つため、`望ましい回答 / 望ましくない回答` のペアで学習するDPOがデータの性質と一致する。教師ありチューニング（SFT）は単一の正解を模倣させるため、複数の許容解がある局面で誤った学習をし、また何切る問題集特有の「意外な正解」への偏りを過学習しやすい。

### 1.3 成功基準

- 手持ちの300問のうち **30問を学習から除外**（ホールドアウト）
- そのテストセットに対する **正解率 90% 以上** を成功とする
- 正解率の定義は [7.2](#72-指標の定義) で定める

> **前提の確認**: 麻雀に絶対的な正解は存在しないが、**本プロジェクトで問題化したものには一応の正解が定められている**。したがって「正解打牌との完全一致率（厳格正解率）」を主指標として採用できる。ただし問題の難易度分布によって達成可能性は変わるため、テストセットの分割は固定し、評価の再現性を担保する（[7.3](#73-テストセットの選定)）。

### 1.4 非ゴール

- 実戦のリアルタイム支援（対局中の自動入力・画面認識）
- 押し引き判断・リーチ判断に特化した機能
- 捨て牌読み・場況判断（**捨て牌は入力対象外**）
- 食い替えの判定（[3.6](#36-副露の表記)）
- 三人麻雀への対応（初期リリースは四人麻雀のみ）

---

## 2. スコープ

既存アプリ `mahjong416` への機能追加。ただし **既存機能への影響を最小化する**ことを制約とし、以下の構成を取る。

- **バックエンド**: 新規に API を用意し、推論ロジック・ソルバー・Vertex AI 呼び出しをすべてそこに閉じる
- **フロントエンド**: 新しいタブコンポーネントを追加し、**API を呼ぶだけ**にする。既存コンポーネント（`mahjong-calc`, `mahjong-today` 等）には一切手を入れない
- **GCPプロジェクト**: 既存の `mahjong416` を使用（Vertex AI API を追加で有効化）

---

## 3. 入出力仕様

### 3.1 牌の表記法

| 種類 | 表記 | 例 |
|---|---|---|
| 萬子 | `1m` 〜 `9m` | `1m`, `5m`, `9m` |
| 筒子 | `1p` 〜 `9p` | `3p`, `7p` |
| 索子 | `1s` 〜 `9s` | `2s`, `8s` |
| 風牌 | `東` `南` `西` `北` | `東`, `北` |
| 三元牌 | `白` `発` `中` | `白`, `中` |
| 赤ドラ | `赤` + 牌 | `赤5m`, `赤5p`, `赤5s` |

- 赤ドラは `赤5m` のように1トークンの牌として扱う。`5m` とは別の牌として表記するが、**役の判定上は `5m` と同一**
- 手牌の並びは「萬子 → 筒子 → 索子 → 風牌（東南西北） → 三元牌（白発中）」の昇順に正規化する
- 同種の牌で通常牌と赤牌が併存する場合、赤牌を後に置く（例: `5m 赤5m`）

### 3.2 入力項目

| 項目 | 必須 | 内容 | 例 |
|---|---|---|---|
| 局 | ✓ | 東1〜4局 / 南1〜4局 | `南2局` |
| 自風 | ✓ | 東家 / 南家 / 西家 / 北家 | `西家` |
| 巡目 | ✓ | 1〜18 | `7巡目` |
| ドラ | ✓ | ドラ表示牌ではなく**ドラそのもの**を1〜複数指定 | `5s` |
| 手牌 | ✓ | **ツモ牌を含まない純手牌**。副露も除く | `2m3m4m 5p6p7p 赤5p 3s3s 東東 白` |
| ツモ牌 | — | 直前にツモった牌。**副露直後は省略する** | `5s` |
| 副露 | — | 自分の副露のみ。なければ空（[3.6](#36-副露の表記)） | `ポン:東東東` |

ツモ牌を手牌と分けて保持する理由:

- 何切る問題の原典が「13枚＋ツモ牌」の形式であり、そのまま受けられればデータ入力時の転記ミスが減る
- プロンプトでツモ牌を明示でき、モデルが「ツモ切り」を選択肢として認識できる
- **ポン・チーの直後（ツモ牌が存在しない局面）**を正しく表現できる

ソルバーは `[...手牌, ツモ牌]` を結合した多重集合しか見ないため、この分離による計算上の影響はない。

**入力対象外**（明示的に扱わない）:

- 捨て牌（自分・他家とも）
- 点棒状況・順位状況
- 他家のリーチ / 副露状況
- 本場・供託

**前提ルール**: 半荘戦、Mリーグルール（**赤あり・一発裏あり**、食いタンあり、後付けあり）

### 3.3 手牌枚数のバリデーション

`手牌の枚数 + 副露数 × 3 + (ツモ牌があれば 1) = 14` を満たすこと。

| 副露数 | 手牌の枚数（ツモ牌あり） | 手牌の枚数（副露直後・ツモ牌なし） |
|---|---|---|
| 0 | 13 | — |
| 1 | 10 | 11 |
| 2 | 7 | 8 |
| 3 | 4 | 5 |
| 4 | 1 | 2 |

副露が0でツモ牌が無い状態は存在しないため、エラーとする。

加えて以下を検証する。

- 同一牌が5枚以上存在しない（手牌・ツモ牌・副露を合わせて、赤5mと5mを含む `5m` 系で4枚以内）
- 赤牌は各色1枚まで
- ドラ指定牌が正当な牌である
- 副露の形が正当（ポン=同一牌3枚、チー=同色連続3枚、カン=同一牌4枚）

### 3.4 出力項目

| 項目 | 内容 | 出所 |
|---|---|---|
| 推奨打牌 | 1つ | LLM |
| 理由 | 推奨打牌の根拠（自然文） | LLM |
| シャンテン数 | 打牌後のシャンテン数 | **ソルバー** |
| 受入枚数 | 打牌後の受け入れ枚数と受入牌の内訳 | **ソルバー** |
| 次善手・許容打牌 | 0〜2件。打牌とシャンテン数・受入枚数、および牌効率観点の定型コメント | **ソルバー**（[3.5](#35-次善手の算出方針)） |

> 表の「出所」は、その項目を**LLMが生成するのか、ソルバーが算出するのか**を示す。ソルバー由来の項目はLLMの出力を採用せず、必ず計算値で確定させる。

#### 「避けるべき打牌」は出力しない

モデルの出力は **推奨打牌とその理由だけ**とする。

悪手データ（`rejected`）の理由文は「**その打牌を選んでしまう典型的な誤った思考過程**」として書く（[6.5.3](#653-理由文)）。これは dispreferred 側の【理由】としては正しいが、preferred 側に「避けるべき打牌」の節を置くと、そこには逆向きの「**なぜその打牌が誤りか**」という別の文章が必要になる。悪手1件につき2種類の文章を書くことになり、データ作成の工数が倍になる。

効果に見合わないため、**出力から「避けるべき打牌」を外し、悪手は dispreferred 側の推奨打牌としてのみ表現する**。DPOは preferred と dispreferred の対比で学習するため、悪手を preferred 側に書かなくても「その打牌を選ばない」ことは学習できる。

### 3.5 次善手の算出方針

既存300問に「許容される他の打牌（`acceptable`）」は**含まれていない**。学習データに存在しないものをLLMに生成させると、根拠のない次善手を作り出す（幻覚）リスクが高い。

そのため **次善手はLLMに生成させず、ソルバーの計算結果から機械的に決定する**。

- ソルバーの全打牌候補（[5.2](#52-出力)）のうち、**推奨打牌と同じシャンテン数**で、**受入枚数が推奨打牌の85%以上**のものを、受入枚数の降順に最大2件
- 添えるコメントは「シャンテン数・受入枚数の差分」を述べる**定型文**のみとし、打点や場況に踏み込んだ評価はしない（根拠がないため）
- 該当がなければ次善手は**0件**として空で返す

これにより、要件である「次善手・許容打牌の提示」を、データの裏付けがない推測を混ぜずに満たす。

### 3.6 副露の表記

副露は「種別 + `:` + 牌の連結」で表す。牌の並びは [3.1](#31-牌の表記法) の昇順規則に従う。

| 種別 | 表記例 | API の `type` |
|---|---|---|
| ポン | `ポン:東東東` | `pon` |
| チー | `チー:3m4m赤5m` | `chi` |
| 明カン | `明カン:白白白白` | `minkan` |
| 暗カン | `暗カン:9s9s9s9s` | `ankan` |
| 加カン | `加カン:2p2p2p2p` | `kakan` |

- 複数の副露は**半角スペース区切り**で並べる: `ポン:東東東 チー:3m4m5m`
- 副露が無い場合は `なし`
- 赤ドラは牌の表記をそのまま使う（`チー:3m4m赤5m`）
- **表示は日本語、APIの `type` は英語**とする。何切る問題の原典や解説が日本語の語彙で書かれており、プロンプト上はそのほうがモデルにとって自然なため
- 加カンはシャンテン数の計算上は明カンと同一に扱うが、局面をそのまま表現できるよう型としては区別する

#### 食い替えは扱わない

Mリーグルールでは食い替えが禁止されているが、本プロジェクトでは**食い替えの判定を行わない**。

判定には「チーで実際に鳴いた牌」の情報が必要になるが、これが影響するのは**チーの直後（ツモ牌なし）の打牌選択に限られる**。何切る問題でこの局面が出る頻度は低く、入力項目とデータ作成の負担に見合わないため、`calledTile` 相当の情報は持たない。

---

## 4. アーキテクチャ

### 4.1 全体構成

```
┌─────────────────────────────────────────────┐
│ フロントエンド（既存 mahjong416 / Lit）         │
│   mahjong-menu                              │
│     └─ mahjong-nanikiru  ← 新規追加のみ        │
└──────────────────┬──────────────────────────┘
                   │ HTTPS POST /nanikiru
                   ▼
┌─────────────────────────────────────────────┐
│ バックエンド API（新規）                        │
│  1. 入力バリデーション・正規化                   │
│  2. ソルバー: 全打牌候補のシャンテン数・受入計算    │
│  3. プロンプト構築（計算結果を事実として注入）       │
│  4. Vertex AI: チューニング済みモデル呼び出し      │
│  5. レスポンスのパース・整形・検証                │
└──────────────────┬──────────────────────────┘
                   │
                   ▼
        Vertex AI（tuned Gemini 2.5 Flash）
```

### 4.2 バックエンドの実行環境

**採用: Cloud Functions (2nd gen)**

| 観点 | 判断 |
|---|---|
| 既存構成との整合 | 既存が Firebase Hosting + Firestore のため、Firebase の枠内で完結する |
| デプロイ分離 | `firebase.json` の functions ターゲットを追加するだけで、Hosting のデプロイと独立して行える |
| コールドスタート | 推論のレイテンシ（数秒）に比べて無視できる |
| ランタイム | Node.js 22 / TypeScript（既存と同じ言語で統一し、ソルバーの型定義を共有可能） |

Cloud Run は将来的にソルバーが重くなった場合の移行先として残す。

### 4.3 ディレクトリ構成

既存ソースへの影響を避けるため、新規ディレクトリに隔離する。

```
mahjong416/
├── src/                          # 既存（変更なし）
│   └── components/
│       └── mahjong-nanikiru.ts   # 新規: フロント（API を呼ぶだけ）
├── functions/                    # 新規: バックエンド
│   ├── src/
│   │   ├── index.ts              # エントリポイント
│   │   ├── api/
│   │   │   ├── handler.ts        # HTTP ハンドラ
│   │   │   └── validate.ts       # 入力バリデーション
│   │   ├── solver/               # ソルバー（純粋関数・依存なし）
│   │   │   ├── tile.ts           # 牌の表記パース・正規化
│   │   │   ├── shanten.ts        # シャンテン数計算
│   │   │   ├── ukeire.ts         # 受け入れ枚数計算
│   │   │   └── candidates.ts     # 全打牌候補の評価
│   │   ├── prompt/
│   │   │   ├── build.ts          # プロンプト構築
│   │   │   └── parse.ts          # 出力パース
│   │   └── vertex/
│   │       └── client.ts         # Vertex AI クライアント
│   ├── package.json              # 独立した依存管理
│   └── tsconfig.json
└── data/                         # 新規: 学習データ
    ├── problems/                 # 問題マスタ（1問1ファイル）
    │   └── 0001.json
    └── build/                    # 生成物（gitignore）
        ├── train.jsonl
        └── validation.jsonl
```

> `functions/` は独立した `package.json` を持つため、既存のビルド（`npm run build` / `build:deploy`）には影響しない。

---

## 5. ソルバー設計

### 5.1 責務

LLMに計算をさせない。**全打牌候補について機械的に正しい数値を算出し、プロンプトに事実として注入する**。

対象は以下。

| 項目 | ソルバーが計算するか | 理由 |
|---|---|---|
| シャンテン数 | ✓ | 数え上げであり、LLMは誤る |
| 受け入れ枚数 | ✓ | 同上 |
| **打牌後のドラ枚数** | ✓ | 打点判断の主要な根拠。赤ドラを含めて数える |
| 役・翻数 | — | 実装コストが大きく、フェーズを分ける。モデルに委ねる |

> **経緯**: 当初はシャンテン数と受け入れ枚数のみを計算していたが、ベースライン測定でモデルが「場風牌の対子を役牌1翻」と誤る例が観測された。打点は判断の根拠になるため、まず機械的に数えられるドラ枚数だけを渡すことにした。役の判定は別フェーズとして残す。

### 5.2 出力

入力の手牌（14枚相当）に対し、打牌可能な各牌について以下を返す。

```ts
type Candidate = {
  discard: Tile;          // 打牌
  shanten: number;        // 打牌後のシャンテン数（和了=-1, テンパイ=0）
  ukeire: {
    tile: Tile;           // 有効牌
    count: number;        // 残り枚数（自分の手牌とドラ表示牌を除いた枚数）
  }[];
  ukeireTotal: number;    // 有効牌の合計枚数
};
```

### 5.3 アルゴリズム

- **シャンテン数**: 面子手・七対子・国士無双の3つを個別に計算し最小値を採用。面子手は再帰的な面子/搭子/対子の分解による標準アルゴリズム
- **受け入れ枚数**: 打牌後の13枚に対し、34種すべてを1枚加えてシャンテン数が減少するかを判定。減少する牌を有効牌とし、残り枚数を合計
- **残り枚数の算定**: `4 - (自分の純手牌にある枚数 + 副露にある枚数)`。**捨て牌は入力対象外のため考慮しない**
- **赤ドラの扱い**: シャンテン数・受け入れの計算では通常牌と同一視し、打点評価のためのメタ情報として別に保持

### 5.4 テスト

ソルバーは純粋関数であり、正解が一意に決まるため単体テストで厳密に検証できる。

- シャンテン数の既知ケース（和了形、テンパイ、七対子、国士無双、四暗刻形など）
- 受け入れ枚数の既知ケース
- 境界値（副露4つで純手牌2枚、赤ドラ複数など）
- 総枚数・同一牌5枚などの不正入力

---

## 6. データパイプライン

### 6.1 方針

300問は**自然言語で少しずつ提供される**。これを段階的に構造化データへ変換する2段構成とする。

```
① 自然言語の問題提供（ユーザー）
        ↓  Claude Code スキル: nanikiru-data-entry
② 問題マスタ JSON（data/problems/NNNN.json）  ← 人間がレビュー可能な正規形式
        ↓  変換スクリプト: npm run build:dataset
③ DPO 用 JSONL（train.jsonl / validation.jsonl）
        ↓  gsutil
④ Cloud Storage
        ↓
⑤ チューニングジョブ
```

②を中間形式として明示的に持つ理由:

- 自然言語からの変換ミスを人間がレビューできる
- プロンプト形式やDPOのペア生成ロジックを変更しても、**②から再生成できる**（①のやり直しが不要）
- ホールドアウトの分割、統計（シャンテン数分布・巡目分布）の集計ができる

### 6.2 問題マスタのスキーマ

`data/problems/NNNN.json`

```json
{
  "id": "0001",
  "situation": {
    "round": "東1局",
    "seat": "南家",
    "turn": 7,
    "dora": ["5s"],
    "hand": ["2m","3m","4m","5p","6p","赤5p","3s","3s","東","東","白","1m","9m"],
    "draw": "9m",
    "melds": []
  },
  "answer": {
    "discard": "9m",
    "reason": "..."
  },
  "rejected": [
    { "discard": "白", "reason": "..." },
    { "discard": "東", "reason": "..." }
  ],
  "meta": {
    "split": "train",
    "source": "...",
    "createdAt": "2026-09-17",
    "reviewed": true
  }
}
```

| フィールド | 必須 | 説明 |
|---|---|---|
| `answer` | ✓ | 正解の打牌と理由 |
| `rejected` | — | 悪手。**空でよい**。空の場合はソルバーの答えから自動生成する（[6.5](#65-rejected悪手データの生成とペア生成ロジック)）。書かれていればそちらを優先する |
| `meta.split` | ✓ | `train` / `validation` / `test`（[7.1](#71-データ分割)）。テストは30問 |
| `meta.reviewed` | ✓ | 人間レビュー済みか。`false` の問題は変換対象から除外 |

> **確定事項**: 既存300問に `rejected`（明確な悪手とその理由）は**含まれていない**。ソルバーの答えをそのまま悪手として使うことで、人手をかけずに生成する（[6.5](#65-rejected悪手データの生成とペア生成ロジック)）。データ入力では正解と理由だけを登録すればよい。

### 6.3 データ入力スキル（`nanikiru-data-entry`）

Claude Code のスキルとして実装する。

**責務**:

1. 自然言語の問題文から `situation` を抽出し、牌表記を正規化する
2. 枚数・重複のバリデーション（[3.3](#33-手牌枚数のバリデーション)）を行い、不整合があればユーザーに差し戻す
3. ソルバーを実行し、正解打牌のシャンテン数・受入枚数を算出して `reason` の妥当性をクロスチェックする
4. `rejected` が未指定の場合、ソルバー結果から「シャンテン数が増える打牌」「受入が大幅に減る打牌」を悪手候補として提案する
5. 連番IDを採番して `data/problems/NNNN.json` を書き出す

**設計上の注意**: スキルは**牌の枚数や受入枚数を自分で数えない**。必ずソルバーを呼ぶ。LLMの計算ミスがデータに混入すると学習データそのものが汚染されるため。

### 6.4 DPO 用 JSONL 形式

Vertex AI のプリファレンス チューニングの形式に従う。

```json
{
  "system_instruction": { "parts": [{ "text": "<システム指示>" }] },
  "contents": [
    { "role": "user", "parts": [{ "text": "<局面＋ソルバー結果>" }] }
  ],
  "completions": [
    { "score": 1, "completion": { "role": "model", "parts": [{ "text": "<望ましい回答>" }] } },
    { "score": 0, "completion": { "role": "model", "parts": [{ "text": "<望ましくない回答>" }] } }
  ]
}
```

**仕様上の制約**:

- 1ペアに「望ましい回答1つ（preferred）」と「望ましくない回答1つ（dispreferred）」が必要
- `score` の取り得る値は **0 または 1 のみ**
  - **`score: 1` = 望ましい回答（preferred）**
  - **`score: 0` = 望ましくない回答（dispreferred）**
  - スコアの値自体はチューニングの挙動を変えない。良い回答と悪い回答を識別するためのラベルとしてのみ機能する
- 入力＋出力の合計トークンが 128,000 以下
- `contents` はユーザーターンで終わること
- マルチモーダル非対応（テキストのみ）

> **確認済み**: `score` の意味づけは英語版原文で確認した。原文の記述は *"The score field indicates whether the completion is preferred or dispreferred. The only possible value is zero or one. Zero represents the dispreferred completion, while one is the preferred completion."* および *"The scores only serve the purpose of identifying good and bad responses. The value of the score itself doesn't change the tuning behavior."*
>
> なお、`completions` 配列内の**並び順に意味はない**（`score` で判別される）が、可読性のため生成スクリプトでは preferred を先に出力する。

### 6.5 rejected（悪手データ）の生成とペア生成ロジック

既存300問に `rejected` は含まれない。**ソルバーの答えをそのまま悪手として使う**ことで、人手をかけずに生成する。

#### 6.5.1 なぜソルバーの答えが悪手になるのか

ソルバーは牌効率（シャンテン数・受け入れ枚数）だけで最良手を選び、**打点も最終形も見ていない**。麻雀では牌効率だけで最善手が決まることは少ないため、ソルバーの答えは「もっともらしいが浅い」選択になる。

これは悪手として理想的な性質を持つ。

- **正解と紙一重**である。明らかな悪手（シャンテン戻しなど）より、「牌効率だけで決めるな」という対比が鮮明になる
- **人手が要らない**。打牌の選定も理由文もソルバーの計算から機械的に決まる
- **正解とペアで意味を持つ**。正解の理由文が「なぜ牌効率だけでは足りないか」を語るため、対比が成立する

#### 6.5.2 悪手の決め方

1. ソルバーの**最良手**（打牌候補の先頭）を悪手にする
2. 最良手が正解と一致する場合は、**次の候補**を悪手にする
3. 打牌候補が正解しか無い場合は悪手を作れないため、その問題は学習データから除外する

問題マスタに `rejected` が書かれている場合はそちらを優先する。自動生成で不適切な悪手になる問題を、個別に上書きできるようにするため。

#### 6.5.3 理由文

**牌効率だけを根拠にした浅い理由文**を、ソルバーの数値から組み立てる。正解の打牌には触れない（悪手側の出力に正解が現れると対比が崩れるため）。

| 悪手と正解の関係 | 理由文の骨子 |
|---|---|
| 悪手のほうがシャンテン数が小さい | 手が一段進むことを根拠にする |
| シャンテン数は同じで受入が多い | 受け入れの広さを根拠にする |
| シャンテン数も受入も同じ | 「牌効率に差がないのでどちらでも同じ」と判断する |
| 受入がわずかに少ない | シャンテン数が変わらないことを根拠にする |

3つ目が最も重要である。受け入れが同数で最終形だけが違う局面こそ、牌効率では決められない判断の典型だからである。

**実例**（問題0001、東1局 西家 7巡目 ドラ南）:

| | 打牌 | 理由 |
|---|---|---|
| preferred | 2s | 中のポンテンを見つつ最終形を重視し、最も広い形に受ける。1s切りと2s切りは受け入れ枚数が同じだが、2s切りのほうがシャンポン待ちになったときに和了りやすく、テンパネの可能性でも差が出る。 |
| dispreferred | 1s | 打1sでも受け入れは18枚で同じ。牌効率に差がないので、どちらを切っても変わらないと判断した。 |

#### 6.5.4 ペア生成

1問から1ペアを作る。

- **preferred（`score: 1`）**: 正解の打牌と、問題マスタの理由
- **dispreferred（`score: 0`）**: 悪手の打牌と、[6.5.3](#653-理由文) の理由

> **重要**: dispreferred側も**フォーマットは完全に正規形を守る**こと。フォーマットが崩れていると、モデルが「内容の良し悪し」ではなく「フォーマットの崩れ」を学習してしまい、判断層の改善につながらない。シャンテン数・受入枚数の数値も、その悪手打牌に対する**ソルバーの正しい計算値**を入れる（数値を間違えたものをdispreferredにすると、学習対象が数値の正しさにすり替わる）。

300問で **約300ペア**。Googleは「まず100サンプルから始め、量より質が重要」としており、件数としては十分な水準にある。

> **既知のリスク**: ソルバーの答えが実際には許容手（`acceptable`）であるケースでは、正しい打牌を悪手として学習させてしまう。ただし牌効率だけで最善手が決まる局面は少なく、影響は限定的と判断した。問題ごとに `rejected` を明示すれば上書きできる。

### 6.6 出力フォーマット

学習データのchosen/rejectedと、推論時の出力を同一フォーマットに固定する。パースの安定性を優先し、JSONではなく**見出し付きの構造化テキスト**とする（JSONは自由記述の理由文でエスケープ事故が起きやすく、DPOでは形式より内容を学習させたいため）。

```
【推奨打牌】9m
【シャンテン数】1シャンテン
【受入】20枚（1m:3 4m:3 7p:4 3s:2 東:2 白:3 ...）
【理由】
...
```

preferred と dispreferred はこの同じ形式で、**推奨打牌と理由の中身だけが異なる**。

| | 推奨打牌 | 理由 |
|---|---|---|
| preferred | `answer.discard` | `answer.reason`（正解を選ぶ根拠） |
| dispreferred | `rejected[i].discard` | `rejected[i].reason`（その打牌を選んでしまう誤った思考） |

シャンテン数・受入は、**それぞれの打牌に対するソルバーの正しい計算値**を入れる。数値を間違えたものを dispreferred にすると、学習対象が数値の正しさにすり替わるため（[6.5.3](#653-ペア生成)）。

**「避けるべき打牌」の節は置かない**（[3.4](#34-出力項目)）。

**次善手のセクションは学習データに含めない。** `acceptable` のデータが存在せず、含めると根拠のない次善手を生成するよう学習させてしまうため。次善手はAPI側でソルバー結果から付加する（[3.5](#35-次善手の算出方針)、[8.5](#85-出力の後処理)）。

---

## 7. 評価設計

### 7.1 データ分割

| 用途 | 件数 | 説明 |
|---|---|---|
| 学習 | 約240問 | DPOペアに変換 |
| 検証（validation） | 約30問 | チューニングジョブに渡し、学習の進行を監視 |
| テスト（holdout） | 30問 | **学習にも検証にも一切使わない**。最終評価専用 |

> 成功基準で指定された「30問を入力として与えない」はテストセットに相当する。Vertex AI は検証データセットの併用を強く推奨しているため、学習用240問からさらに30問を検証用に分ける構成を提案する。これにより学習に使えるのは約240問となる。

### 7.2 指標の定義

| 指標 | 定義 | 目標 |
|---|---|---|
| **厳格正解率** | 推奨打牌が `answer.discard` と完全一致した割合 | **90%以上（成功基準）** |
| 準最善一致率 | 推奨打牌が、ソルバー上で最良手と同シャンテンかつ受入85%以上の候補に含まれた割合 | 参考値 |
| 悪手回答率 | 推奨打牌が `rejected[].discard` に一致した割合 | 0%を目指す |
| 数値整合率 | 出力のシャンテン数・受入枚数がソルバー結果と一致した割合 | 100%（不一致はAPI側で機械的に修正） |
| 理由の妥当性 | 理由文が推奨打牌の根拠として成立しているか（人手またはLLM-as-a-judge） | 参考値 |

### 7.3 テストセットの選定

問題化されたものには一応の正解が定められているため、**テスト30問は無作為抽出とし、難易度による選別は行わない**。恣意的な選別は評価を甘くするため。

- 抽出は**固定シードによる乱数**で行い、再現可能にする
- ただしシャンテン数・巡目・副露有無の分布が学習セットから大きく偏っていないことを**抽出後に確認**する。偏りが大きければシードを変えて再抽出し、その事実を記録する（抽出後に個別の問題を差し替えることはしない）
- 分割は `meta.split` として**固定し、以降変更しない**（変更すると評価が信用できなくなる）
- 厳格正解率と併せて**準最善一致率も報告**する。正解を外した場合でも「牌効率上は妥当な範囲を外していないか」を把握できるため

### 7.4 ベースライン

チューニングの効果を判定するため、以下を同一テストセットで測る。

1. 素の Gemini 2.5 Flash（プロンプトのみ、ソルバー結果なし）
2. 素の Gemini 2.5 Flash（プロンプト＋ソルバー結果）
3. チューニング済みモデル（プロンプト＋ソルバー結果）

1→2 の差が**ソルバー導入の効果**、2→3 の差が**チューニングの効果**であり、これを分離して測れる形にする。

---

## 8. API 仕様

### 8.1 エンドポイント

```
POST /nanikiru
Content-Type: application/json
```

### 8.2 リクエスト

```json
{
  "round": "東1局",
  "seat": "南家",
  "turn": 7,
  "dora": ["5s"],
  "hand": ["2m","3m","4m","5p","6p","赤5p","3s","3s","東","東","白","1m","9m"],
  "draw": "9m",
  "melds": []
}
```

`draw` は副露直後のみ省略できる。

`melds` の要素:

```json
{ "type": "pon" | "chi" | "minkan" | "ankan" | "kakan", "tiles": ["東","東","東"] }
```

`tiles` は昇順に正規化する。表示用の文字列（`ポン:東東東`）への変換はバックエンドで行う（[3.6](#36-副露の表記)）。

### 8.3 レスポンス

```json
{
  "recommended": {
    "discard": "9m",
    "shanten": 1,
    "ukeire": { "total": 20, "tiles": [{ "tile": "1m", "count": 3 }] },
    "reason": "..."
  },
  "alternatives": [
    { "discard": "1m", "shanten": 1, "ukeire": { "total": 18, "tiles": [] }, "note": "推奨打牌と同じ1シャンテンだが、受入が2枚少ない" }
  ],
  "solver": {
    "candidates": [
      { "discard": "9m", "shanten": 1, "ukeireTotal": 20 }
    ]
  }
}
```

`alternatives` は LLM の出力ではなく、**ソルバー結果から機械的に生成**した次善手（[3.5](#35-次善手の算出方針)）。そのため理由は自然文の `reason` ではなく定型の `note` とする。

`solver.candidates` は全打牌候補の計算結果であり、LLMの出力とは独立してソルバーが算出した値をそのまま返す。フロントで一覧表示でき、またモデルの出力を検証する根拠にもなる。

### 8.4 エラー

| コード | 条件 |
|---|---|
| 400 | バリデーションエラー（枚数不正、牌表記不正、同一牌5枚など） |
| 429 | レート制限超過 |
| 502 | Vertex AI 呼び出し失敗 |
| 504 | タイムアウト |

### 8.5 出力の後処理

LLMの出力は以下の検証を通してから返す。

1. **推奨打牌が手牌に存在するか** — 存在しなければソルバーの最良手にフォールバックし、警告フラグを立てる
2. **シャンテン数・受入枚数がソルバー結果と一致するか** — 不一致ならソルバーの値で上書きする（LLMの数値を信用しない）
3. **フォーマットのパース成功** — 失敗時は1回リトライし、再失敗ならソルバー結果のみを返す
4. **次善手の付加** — LLMの出力には含まれないため、ソルバー結果から [3.5](#35-次善手の算出方針) の基準で `alternatives` を組み立てて返す

---

## 9. 運用・コスト

### 9.1 コスト

| 項目 | 見積 |
|---|---|
| チューニング学習 | 学習トークン数 × エポック数で課金。約600ペア × 数百トークン × 1〜2エポックで**数百〜数千円**のオーダー |
| 推論 | Gemini 2.5 Flash の通常課金。プロンプトに300問を積む必要がないため、**1リクエストのトークン数は数千程度**に収まる |
| Cloud Functions | 個人利用の規模では無料枠内に収まる想定 |

チューニング方式は、300問を毎回プロンプトへ投入するRAG方式（約9万トークン/リクエスト）に比べ、ランニングコストとレイテンシの両面で有利。

### 9.2 モデルのライフサイクル

**最大の運用課題**。ベースモデルが更新・廃止されるたびに再チューニングが必要になる。

- 対応モデルは **Gemini 2.5 Flash / 2.5 Flash-Lite**（プリファレンス チューニングの対応範囲）
- モデルのバージョンとチューニング済みエンドポイントを設定値として外部化し、差し替えを容易にする
- `data/problems/` を正本として保持することで、**いつでも再チューニングできる状態**を維持する（6.1の②を持つ最大の理由）

### 9.3 セキュリティ

- Vertex AI の認証は Cloud Functions のサービスアカウント経由。**クライアントに認証情報を渡さない**
- API にレート制限を設ける（悪用による課金増の防止）
- 入力は牌の組み合わせのみで個人情報を含まないが、既存アプリと同じ認証方式に合わせるか要確認（[11](#11-未決事項) 参照）

---

## 10. 実装フェーズ

| Phase | 内容 | 完了条件 |
|---|---|---|
| **0. ベースライン測定** | 手持ちの問題から20〜30問で素のGeminiを評価し、誤りを「計算ミス」「判断ミス」に分類 | 誤りの内訳が定量化されている |
| **1. ソルバー実装** | シャンテン数・受入枚数の計算と単体テスト | 既知ケースが全て通る |
| **2. データ入力基盤** | 問題マスタのスキーマ確定、`nanikiru-data-entry` スキル実装 | 数問を自然言語から投入できる |
| **3. データ整備** | 300問を問題マスタへ。**`rejected` の新規生成**（ソルバー抽出→理由文起草→人間レビュー）。最大の工数 | 300問が `reviewed: true`、各問に `rejected` が1件以上 |
| **4. 変換 + チューニング** | JSONL変換、GCSアップロード、チューニングジョブ実行 | チューニング済みエンドポイントが動く |
| **5. 評価** | テスト30問で3条件を比較 | 厳格正解率が測定され、成功基準を判定できる |
| **6. API実装** | Cloud Functions のエンドポイント、後処理・エラー処理 | APIが仕様通り応答する |
| **7. フロント実装** | `mahjong-nanikiru` コンポーネント追加 | 既存機能に影響なくタブが動く |

Phase 0 を最初に置く理由: **誤りが計算ミス主体だった場合、ソルバー導入（Phase 1）だけで目標に到達する可能性があり、Phase 3 の重い作業が不要になる**。逆に判断ミス主体であればチューニングへの投資が正当化される。ここを測らずに進むと、直らない問題にデータ整備コストを投じるリスクがある。

---

## 11. 未決事項

レビューで判断が必要な項目。

| # | 項目 | 内容 |
|---|---|---|
| 1 | **rejected の目標件数** | 1問あたり最大3件を想定しているが、レビュー工数との兼ね合いで1〜2件に絞るか（[6.5](#65-rejected悪手データの生成とペア生成ロジック)） |
| 2 | **次善手の受入しきい値** | ソルバーからの抽出基準を「同シャンテン・受入85%以上」としているが、この値は実データを見て調整が必要（[3.5](#35-次善手の算出方針)） |
| 3 | **APIの認証方式** | 既存アプリの認証状況に合わせるか、新たに設ける必要があるか |
| 4 | **ドラの入力形式** | 「ドラそのもの」を入力する前提にしているが、UI上は「ドラ表示牌」を入力したいか |
| 5 | **フロントのUI設計** | 牌の選択方法（牌画タップ / テキスト入力）、既存アプリのMaterial Webコンポーネントとの整合 |

### 解決済み（本版で反映）

| 項目 | 結論 |
|---|---|
| 既存300問の `rejected` の有無 | **含まれない**。新規生成する方針を [6.5](#65-rejected悪手データの生成とペア生成ロジック) に定義 |
| 成功基準90%の妥当性 | 問題化されたものには一応の正解があるため、**厳格正解率90%を主指標として採用**。テストは無作為抽出（[7.3](#73-テストセットの選定)） |
| `score` の意味づけ | 英語版原文で確認。**`score: 1` = preferred / `score: 0` = dispreferred**（[6.4](#64-dpo-用-jsonl-形式)） |
| `acceptable` の有無 | **含まれない**。次善手はLLMに生成させず、**ソルバー結果から機械的に付加**する方針に変更（[3.5](#35-次善手の算出方針)） |

---

## 参考資料

- [プリファレンス チューニングについて](https://docs.cloud.google.com/vertex-ai/generative-ai/docs/models/gemini-preference-tuning?hl=ja)
- [プリファレンス チューニング データの準備](https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/tuning/preference-tuning/prepare-data?hl=ja)
- [教師ありファインチューニングについて](https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/tuning/supervised-tuning?hl=ja)
- [強化学習ファインチューニングについて](https://docs.cloud.google.com/gemini-enterprise-agent-platform/models/tuning/reinforcement-tuning?hl=ja)
