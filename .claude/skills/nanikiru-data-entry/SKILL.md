---
name: nanikiru-data-entry
description: 何切る問題を自然言語で受け取り、学習データ（data/problems/NNNN.json）として登録するスキル。「何切るの問題を登録して」「この問題をデータにして」「何切る問題を追加」などの依頼があれば必ず使う。牌の表記を正規化し、ソルバーで検証し、悪手の候補を提案してから書き出す。
---

# 何切る問題のデータ登録

自然言語で提供された何切る問題を、`data/problems/NNNN.json` に登録する。

## 絶対に守ること

**牌の枚数・シャンテン数・受入枚数を自分で数えない。** 必ず `functions` の `npm run check` を実行して、ソルバーの計算結果を使う。LLMの計算ミスが学習データに混入すると、学習データそのものが汚染される（design doc §6.3）。

**不明点は推測しない。** 局・自風・巡目・ドラのいずれかが提供された文章から読み取れない場合は、ユーザーに質問する。

## 手順

### 1. 局面を抽出する

提供された文章から以下を読み取る。

| 項目 | 必須 | 備考 |
| --- | --- | --- |
| 局 | ✓ | `東1局`〜`南4局` |
| 自風 | ✓ | `東家` / `南家` / `西家` / `北家` |
| 巡目 | ✓ | 1〜18 の整数 |
| ドラ | ✓ | **ドラそのもの**。ドラ表示牌で書かれていたら1つ進めた牌に直す |
| 手牌 | ✓ | **ツモ牌を含まない純手牌** |
| ツモ牌 | — | 副露直後のみ省略 |
| 副露 | — | `{"type": "pon", "tiles": [...]}` 形式 |
| 正解の打牌 | ✓ | |
| 正解の理由 | ✓ | 原典の解説をそのまま使わず、要点を自分の言葉でまとめる |

牌の表記は `1m` / `1p` / `1s` / `東南西北白発中` / `赤5m` に正規化する。
`123m` のような略記は1枚ずつに展開する。

### 2. 枚数を確認する

`手牌 + 副露×3 + (ツモ牌があれば1) = 14` を満たすか確認する。

| 副露数 | 手牌（ツモあり） | 手牌（副露直後） |
| --- | --- | --- |
| 0 | 13 | — |
| 1 | 10 | 11 |
| 2 | 7 | 8 |
| 3 | 4 | 5 |
| 4 | 1 | 2 |

合わない場合は書き出さず、ユーザーに差し戻して確認する。

### 3. 下書きを作る

`data/problems/` の既存ファイルを見て**次の連番**を決める（4桁ゼロ埋め）。
`rejected` は空、`meta.reviewed` は `false` で下書きする。

```json
{
  "id": "0001",
  "situation": {
    "round": "東1局",
    "seat": "南家",
    "turn": 7,
    "dora": ["5s"],
    "hand": ["1m","2m","3m","4m","5m","6m","7m","8m","9m","東","東","1p","2p"],
    "draw": "5s",
    "melds": []
  },
  "answer": {"discard": "5s", "reason": "..."},
  "rejected": [],
  "meta": {"split": "train", "source": "...", "createdAt": "YYYY-MM-DD", "reviewed": false}
}
```

`meta.split` は既定で `train`。ユーザーの指示があればそれに従う。

### 4. ソルバーで検証する

```bash
cd functions && npm run check --silent -- ../data/problems/NNNN.json
```

出力を確認する。

- **エラーが出たら書き出しをやり直す。** 牌が重複しすぎている、枚数が合わない、正解の打牌が手牌にない、などが検出される
- 「正解はソルバー上の最良手ではありません」と出た場合、正解の理由が**打点・場況・安全度**に触れているかを確認する。牌効率だけを根拠にしているなら、理由が不十分なのでユーザーに確認する
- 正解のシャンテン数・受入枚数を理由文の裏付けとして使う

### 5. 悪手を確認する

悪手は**ソルバーの答えから自動生成される**ため、通常は何も書かなくてよい。`rejected` は空のままでよい。

`npm run check` の「## 学習データで使われる悪手」を見て、**1点だけ確認する**。

> **自動生成された悪手が、解説の挙げている許容手と一致していないか。**

出典の解説が「Aが正解だが、方針次第ではBもある」と書いていることがある。自動生成がその B を選んでしまうと、**許容手を悪手として学習させてしまう**。

一致していた場合は `rejected` を手動で指定して避ける。選ぶのは、**正解と受け入れ枚数が同等でありながら、その問題で学ばせたい要素を壊す打牌**が望ましい。

理由文は自動生成と同じ調子で、**牌効率だけを根拠にした浅い論理**を書く。「なぜ悪いか」の説明ではない。dispreferred 側の【理由】として使われるため、その打牌を選んでしまう（誤った）論理でなければならない。

```json
"rejected": [
  {"discard": "7p", "reason": "打7pでも受け入れは13枚で同じ。牌効率に差がないので、どれを切っても変わらないと判断した。"}
]
```

一致していなければ**何もしない**。

### 6. 確定する

`meta.reviewed` を `true` にして、もう一度 `npm run check` を通す。
最後にユーザーへ、登録した問題の要約（ID・局面・正解・実際に使われる悪手）を報告する。
悪手を手動指定した場合は、その理由も伝える。

## 参照

- 仕様の詳細: [nanikiru-ai-design-doc.md](../../../nanikiru-ai-design-doc.md) §3（入出力仕様）、§6（データパイプライン）
- 問題マスタの形式: [data/README.md](../../../data/README.md)
